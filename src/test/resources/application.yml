spring:
  jpa:
    database-platform: org.hibernate.dialect.HSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:hsqldb:mem:player-statsdb
    username: sa
    driver-class-name: org.hsqldb.jdbcDriver